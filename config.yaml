whatsapp:
  report_timeout_seconds: 120

telegram:
  report_timeout_seconds: 120
  allow_direct_messages: true

python_service:
  host: "localhost"
  port: 5000

storage:
  database: "sqlite:///data/messages.db"
  export_dir: "./exports"

logging:
  level: "info"
